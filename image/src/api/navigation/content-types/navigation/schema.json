{
  "kind": "singleType",
  "collectionName": "navigation",
  "info": {
    "singularName": "navigation",
    "pluralName": "navigations",
    "displayName": "Navigation",
    "description": "Top-bar navigation. 1:1 with website's NavContent — links array (label, href) + cta label."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "ctaLabel": {
      "type": "string",
      "required": true
    },
    "links": {
      "type": "json",
      "required": true
    }
  }
}
