{
  "kind": "collectionType",
  "collectionName": "packages",
  "info": {
    "singularName": "package",
    "pluralName": "packages",
    "displayName": "Package",
    "description": "Service package shown in the services grid. Mirrors website's Package interface. `features` and `addons` arrays stay as JSON (move to nested components in a follow-up when editor UX warrants)."
  },
  "options": { "draftAndPublish": true },
  "attributes": {
    "name": { "type": "string", "required": true },
    "tagline": { "type": "string", "required": true },
    "price": { "type": "string", "required": true },
    "priceNote": { "type": "string" },
    "highlight": { "type": "boolean", "default": false, "required": true },
    "highlightLabel": { "type": "string" },
    "cta": { "type": "string", "required": true },
    "features": { "type": "json", "required": true },
    "addons": { "type": "json" }
  }
}
